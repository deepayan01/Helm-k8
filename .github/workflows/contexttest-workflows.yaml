name: testing context

on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checking the github context
        run: |
          echo "action-repo: ${{ github.repository }}"
          echo "ref: ${{ github.ref }}"
          echo "event: ${{ github.event_name }}"
      - name: retriving the repo var
        run: |
          echo "variable: ${{ vars.MY_VAR }}"
