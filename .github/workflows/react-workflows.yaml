name: building react application

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: action/checkout@v5
      - name: printing the files
        run: ls -lrt
